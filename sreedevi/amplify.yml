version: 1
applications:
  - appRoot: sd/sreedevi  # Point to the 'sreedevi' folder inside 'sd'
    frontend:
      phases:
        build:
          commands:
            - npm install
            - npm run build